// ============================================================
// GymFuel Shared Package — Calculator Types
// ============================================================

export interface BMICalcResult {
  bmi: number;
  classification: string;
}

export interface ProteinCalcResult {
  min: number;
  max: number;
}

export interface OneRepMaxCalcResult {
  oneRepMax: number;
}
