# ============================================================
# GymFuel — Nginx Staging Configuration
# ============================================================

# HTTP -> HTTPS Redirect
server {
    listen 80;
    server_name staging.rkvnova.com staging-app.rkvnova.com staging-admin.rkvnova.com staging-api.rkvnova.com;
    return 301 https://$host$request_uri;
}

# 1. Landing Page (staging.rkvnova.com)
server {
    listen 443 ssl;
    server_name staging.rkvnova.com;

    ssl_certificate /etc/letsencrypt/live/staging.rkvnova.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/staging.rkvnova.com/privkey.pem;

    location / {
        resolver 127.0.0.11 valid=30s;
        set $upstream http://gymfuel-landing:80;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# 2. User App (staging-app.rkvnova.com)
server {
    listen 443 ssl;
    server_name staging-app.rkvnova.com;

    ssl_certificate /etc/letsencrypt/live/staging.rkvnova.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/staging.rkvnova.com/privkey.pem;

    location / {
        resolver 127.0.0.11 valid=30s;
        set $upstream http://gymfuel-user:80;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# 3. Admin Panel (staging-admin.rkvnova.com)
server {
    listen 443 ssl;
    server_name staging-admin.rkvnova.com;

    ssl_certificate /etc/letsencrypt/live/staging.rkvnova.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/staging.rkvnova.com/privkey.pem;

    location / {
        resolver 127.0.0.11 valid=30s;
        set $upstream http://gymfuel-admin:80;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# 4. Backend API (staging-api.rkvnova.com)
server {
    listen 443 ssl;
    server_name staging-api.rkvnova.com;

    ssl_certificate /etc/letsencrypt/live/staging.rkvnova.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/staging.rkvnova.com/privkey.pem;

    location / {
        resolver 127.0.0.11 valid=30s;
        set $upstream http://gymfuel-api:5000;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
